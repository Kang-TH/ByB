/** NaverMapMarkerOverlay 기본 핀(미지정 시)보다 작게 */
export const NAVER_MAP_MARKER_SIZE = {
  width: 24,
  height: 30,
} as const;

export const NAVER_MAP_MARKER_CAPTION = {
  textSize: 11,
  offset: 2,
} as const;
